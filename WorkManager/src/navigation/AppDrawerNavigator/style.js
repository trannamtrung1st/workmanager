import { StyleSheet } from "react-native";
import { appStyles } from "$styles";

const styles = StyleSheet.create({});
const itemStyle = [appStyles.borderPrimaryGreen];
const s = { itemStyle };

export default s;
